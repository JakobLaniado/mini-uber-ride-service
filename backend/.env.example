# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=mini_uber

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# App
PORT=3000

# OpenRouter (optional — if not set, MockLlmProvider is used)
# OPENROUTER_API_KEY=sk-or-...
